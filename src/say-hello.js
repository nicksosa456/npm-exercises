export const hello = () => 'Hey';
export const add = (x, y) => x + y;
export const subtract = (x, y) => x - y;
export const multiply = (x, y) => x * y;
export const divide = (x, y) => x / y;

// module.exports = {hello, add, subtract, multiply, divide};

// export default {hello, add, subtract, multiply, divide};